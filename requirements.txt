# Everything the six foundation chapters need, in one environment.
# Each chapter's README also lists the smaller subset it uses on its own.
#
# The upper bounds keep this course working as new major versions ship.
# Tested on Python 3.13 with the versions noted in the comments.
pandas>=2.2,<3          # tested 2.3.3
numpy>=2.0,<3           # tested 2.4.6
scikit-learn>=1.5,<2    # tested 1.9.0
scipy>=1.13,<2          # tested 1.17.1
flask>=3.0,<4           # tested 3.1.3
mlflow>=3.0,<4          # tested 3.15.1
